#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first parameter to be multiplied
 * @b: second parameter to be multiplied
 * Return: the result
 */
int mul(int a, int b)
{
	int multi = a * b;

	return (multi);
}
